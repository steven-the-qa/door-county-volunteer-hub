name = "dcvh-cms-auth"
main = "src/index.js"
compatibility_date = "2024-11-01"

# One-time setup — create the namespace, then replace `id` below with what
# it prints (see cms-auth/README.md):
#   npx wrangler kv namespace create OAUTH_RESULTS
[[kv_namespaces]]
binding = "OAUTH_RESULTS"
id = "2a037c3a8b7e4e67a626c15e4c727253"

# Secrets (not stored here):
#   npx wrangler secret put GITHUB_OAUTH_CLIENT_ID
#   npx wrangler secret put GITHUB_OAUTH_CLIENT_SECRET
#
# Deployed URL after `npx wrangler deploy`:
#   https://dcvh-cms-auth.<your-workers-subdomain>.workers.dev
# This must match admin/config.yml's auth_endpoint / base_url, and the
# GitHub OAuth App's callback URL must be <that-url>/callback.
